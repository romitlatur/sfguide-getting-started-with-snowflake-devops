"""
Snowflake Streamlit API.

The Snowflake Streamlit API is a REST API that you can use to access, update, and perform certain actions on Streamlit resources in a Snowflake database.  # noqa: E501

The version of the OpenAPI document: 0.0.1
Contact: support@snowflake.com
Generated by: https://openapi-generator.tech

Do not edit this file manually.
"""

from __future__ import annotations

import json
import pprint
import re  # noqa: F401

from typing import Any, Optional

from pydantic import BaseModel

from snowflake.core.streamlit._generated.models.commit_streamlit_request_version import (
    CommitStreamlitRequestVersion,
    CommitStreamlitRequestVersionModel,
)


class CommitStreamlitRequest(BaseModel):
    """A model object representing the CommitStreamlitRequest resource.

    Constructs an object of type CommitStreamlitRequest with the provided properties.

    Parameters
    __________
    version : CommitStreamlitRequestVersion, optional
    """

    version: Optional[CommitStreamlitRequestVersion] = None

    __properties = ["version"]

    class Config:
        populate_by_name = True
        validate_assignment = True

    def to_str(self) -> str:
        """Returns the string representation of the model using alias."""
        return pprint.pformat(self.dict(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias."""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> CommitStreamlitRequest:
        """Create an instance of CommitStreamlitRequest from a JSON string."""
        return cls.from_dict(json.loads(json_str))

    def to_dict(
        self,
        hide_readonly_properties: bool = False,
    ) -> dict[str, Any]:
        """Returns the dictionary representation of the model using alias."""
        exclude_properties = set()

        if hide_readonly_properties:
            exclude_properties.update({})

        _dict = dict(self._iter(to_dict=True, by_alias=True, exclude=exclude_properties, exclude_none=True))

        # override the default output from pydantic by calling `to_dict()` of version
        if self.version:
            _dict["version"] = self.version.to_dict()

        return _dict

    def to_dict_without_readonly_properties(self) -> dict[str, Any]:
        """Return the dictionary representation of the model without readonly properties."""
        return self.to_dict(hide_readonly_properties=True)

    @classmethod
    def from_dict(cls, obj: dict) -> CommitStreamlitRequest:
        """Create an instance of CommitStreamlitRequest from a dict."""
        if obj is None:
            return None

        if type(obj) is not dict:
            return CommitStreamlitRequest.parse_obj(obj)

        _obj = CommitStreamlitRequest.parse_obj(
            {
                "version": CommitStreamlitRequestVersion.from_dict(obj.get("version"))
                if obj.get("version") is not None
                else None,
            }
        )

        return _obj


class CommitStreamlitRequestModel:
    def __init__(
        self,  # optional properties
        version: Optional[CommitStreamlitRequestVersion] = None,
    ):
        """A model object representing the CommitStreamlitRequest resource.

        Constructs an object of type CommitStreamlitRequest with the provided properties.

        Parameters
        __________
        version : CommitStreamlitRequestVersion, optional
        """
        self.version = version

    __properties = ["version"]

    def __repr__(self) -> str:
        return repr(self._to_model())

    def _to_model(self):
        return CommitStreamlitRequest(
            version=self.version._to_model() if self.version is not None else None,
        )

    @classmethod
    def _from_model(cls, model) -> CommitStreamlitRequestModel:
        return CommitStreamlitRequestModel(
            version=CommitStreamlitRequestVersionModel._from_model(model.version) if model.version else None,
        )

    def to_dict(self):
        """Create a dictionary of the properties from a CommitStreamlitRequest.

        This method constructs a dictionary with the key-value entries corresponding to the properties of the CommitStreamlitRequest object.

        Returns
        _______
        dict
            A dictionary object created using the input model.
        """
        return self._to_model().to_dict()

    @classmethod
    def from_dict(cls, obj: dict) -> CommitStreamlitRequestModel:
        """Create an instance of CommitStreamlitRequest from a dict.

        This method constructs a CommitStreamlitRequest object from a dictionary with the key-value pairs of its properties.

        Parameters
        ----------
        obj : dict
            A dictionary whose keys and values correspond to the properties of the resource object.

        Returns
        _______
        CommitStreamlitRequest
            A CommitStreamlitRequest object created using the input dictionary; this will fail if the required properties are missing.
        """
        return cls._from_model(CommitStreamlitRequest.from_dict(obj))


CommitStreamlitRequest._model_class = CommitStreamlitRequestModel
