"""
Snowflake Sequence API.

The Snowflake Sequence API is a REST API that you can use to access, update, and perform certain actions on Sequence resource in a Snowflake database.  # noqa: E501

The version of the OpenAPI document: 0.0.1
Contact: support@snowflake.com
Generated by: https://openapi-generator.tech

Do not edit this file manually.
"""

from __future__ import annotations

import json
import pprint
import re

from datetime import datetime
from typing import Any, Optional

from pydantic import BaseModel, Field, StrictBool, StrictInt, StrictStr, field_validator
from typing_extensions import Annotated


class Sequence(BaseModel):
    """A model object representing the Sequence resource.

    Constructs an object of type Sequence with the provided properties.

    Parameters
    __________
    name : str
        Name of the sequence
    start : int, optional
        First value returned by the sequence
    increment : int, optional
        Step interval of the sequence
    ordered : bool, optional
        Whether or not the values are generated in increasing or decreasing order
    comment : str, optional
        Comment for the sequence
    created_on : datetime, optional
        Date and time when the sequence was created.
    database_name : str, optional
        Database in which the sequence is stored
    schema_name : str, optional
        Schema in which the sequence is stored
    owner : str, optional
        Role that owns the sequence
    owner_role_type : str, optional
        The type of role that owns the sequence
    """

    start: Optional[StrictInt] = None

    increment: Optional[StrictInt] = None

    ordered: Optional[StrictBool] = None

    comment: Optional[StrictStr] = None

    name: Annotated[str, Field(strict=True)]

    created_on: Optional[datetime] = None

    database_name: Optional[Annotated[str, Field(strict=True)]] = None

    schema_name: Optional[Annotated[str, Field(strict=True)]] = None

    owner: Optional[Annotated[str, Field(strict=True)]] = None

    owner_role_type: Optional[StrictStr] = None

    __properties = [
        "start",
        "increment",
        "ordered",
        "comment",
        "name",
        "created_on",
        "database_name",
        "schema_name",
        "owner",
        "owner_role_type",
    ]

    @field_validator("name")
    def name_validate_regular_expression(cls, v):
        if not re.match(r"""^\"([^\"]|\"\")+\"|[a-zA-Z_][a-zA-Z0-9_$]*$""", v):
            raise ValueError(r"""must validate the regular expression /^"([^"]|"")+"|[a-zA-Z_][a-zA-Z0-9_$]*$/""")
        return v

    @field_validator("database_name")
    def database_name_validate_regular_expression(cls, v):
        if v is None:
            return v
        if not re.match(r"""^\"([^\"]|\"\")+\"|[a-zA-Z_][a-zA-Z0-9_$]*$""", v):
            raise ValueError(r"""must validate the regular expression /^"([^"]|"")+"|[a-zA-Z_][a-zA-Z0-9_$]*$/""")
        return v

    @field_validator("schema_name")
    def schema_name_validate_regular_expression(cls, v):
        if v is None:
            return v
        if not re.match(r"""^\"([^\"]|\"\")+\"|[a-zA-Z_][a-zA-Z0-9_$]*$""", v):
            raise ValueError(r"""must validate the regular expression /^"([^"]|"")+"|[a-zA-Z_][a-zA-Z0-9_$]*$/""")
        return v

    @field_validator("owner")
    def owner_validate_regular_expression(cls, v):
        if v is None:
            return v
        if not re.match(r"""^\"([^\"]|\"\")+\"|[a-zA-Z_][a-zA-Z0-9_$]*$""", v):
            raise ValueError(r"""must validate the regular expression /^"([^"]|"")+"|[a-zA-Z_][a-zA-Z0-9_$]*$/""")
        return v

    class Config:
        populate_by_name = True
        validate_assignment = True

    def to_str(self) -> str:
        """Returns the string representation of the model using alias."""
        return pprint.pformat(self.dict(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias."""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Sequence:
        """Create an instance of Sequence from a JSON string."""
        return cls.from_dict(json.loads(json_str))

    def to_dict(
        self,
        hide_readonly_properties: bool = False,
    ) -> dict[str, Any]:
        """Returns the dictionary representation of the model using alias."""
        exclude_properties = set()

        if hide_readonly_properties:
            exclude_properties.update(
                {
                    "created_on",
                    "database_name",
                    "schema_name",
                    "owner",
                    "owner_role_type",
                }
            )

        _dict = dict(self._iter(to_dict=True, by_alias=True, exclude=exclude_properties, exclude_none=True))

        return _dict

    def to_dict_without_readonly_properties(self) -> dict[str, Any]:
        """Return the dictionary representation of the model without readonly properties."""
        return self.to_dict(hide_readonly_properties=True)

    @classmethod
    def from_dict(cls, obj: dict) -> Sequence:
        """Create an instance of Sequence from a dict."""
        if obj is None:
            return None

        if type(obj) is not dict:
            return Sequence.parse_obj(obj)

        _obj = Sequence.parse_obj(
            {
                "start": obj.get("start"),
                "increment": obj.get("increment"),
                "ordered": obj.get("ordered"),
                "comment": obj.get("comment"),
                "name": obj.get("name"),
                "created_on": obj.get("created_on"),
                "database_name": obj.get("database_name"),
                "schema_name": obj.get("schema_name"),
                "owner": obj.get("owner"),
                "owner_role_type": obj.get("owner_role_type"),
            }
        )

        return _obj


class SequenceModel:
    def __init__(
        self,
        name: str,
        # optional properties
        start: Optional[int] = None,
        increment: Optional[int] = None,
        ordered: Optional[bool] = None,
        comment: Optional[str] = None,
        created_on: Optional[datetime] = None,
        database_name: Optional[str] = None,
        schema_name: Optional[str] = None,
        owner: Optional[str] = None,
        owner_role_type: Optional[str] = None,
    ):
        """A model object representing the Sequence resource.

        Constructs an object of type Sequence with the provided properties.

        Parameters
        __________
        name : str
            Name of the sequence
        start : int, optional
            First value returned by the sequence
        increment : int, optional
            Step interval of the sequence
        ordered : bool, optional
            Whether or not the values are generated in increasing or decreasing order
        comment : str, optional
            Comment for the sequence
        created_on : datetime, optional
            Date and time when the sequence was created.
        database_name : str, optional
            Database in which the sequence is stored
        schema_name : str, optional
            Schema in which the sequence is stored
        owner : str, optional
            Role that owns the sequence
        owner_role_type : str, optional
            The type of role that owns the sequence
        """
        self.start = start
        self.increment = increment
        self.ordered = ordered
        self.comment = comment
        self.name = name
        self.created_on = created_on
        self.database_name = database_name
        self.schema_name = schema_name
        self.owner = owner
        self.owner_role_type = owner_role_type

    __properties = [
        "start",
        "increment",
        "ordered",
        "comment",
        "name",
        "created_on",
        "database_name",
        "schema_name",
        "owner",
        "owner_role_type",
    ]

    def __repr__(self) -> str:
        return repr(self._to_model())

    def _to_model(self):
        return Sequence(
            start=self.start,
            increment=self.increment,
            ordered=self.ordered,
            comment=self.comment,
            name=self.name,
            created_on=self.created_on,
            database_name=self.database_name,
            schema_name=self.schema_name,
            owner=self.owner,
            owner_role_type=self.owner_role_type,
        )

    @classmethod
    def _from_model(cls, model) -> SequenceModel:
        return SequenceModel(
            start=model.start,
            increment=model.increment,
            ordered=model.ordered,
            comment=model.comment,
            name=model.name,
            created_on=model.created_on,
            database_name=model.database_name,
            schema_name=model.schema_name,
            owner=model.owner,
            owner_role_type=model.owner_role_type,
        )

    def to_dict(self):
        """Create a dictionary of the properties from a Sequence.

        This method constructs a dictionary with the key-value entries corresponding to the properties of the Sequence object.

        Returns
        _______
        dict
            A dictionary object created using the input model.
        """
        return self._to_model().to_dict()

    @classmethod
    def from_dict(cls, obj: dict) -> SequenceModel:
        """Create an instance of Sequence from a dict.

        This method constructs a Sequence object from a dictionary with the key-value pairs of its properties.

        Parameters
        ----------
        obj : dict
            A dictionary whose keys and values correspond to the properties of the resource object.

        Returns
        _______
        Sequence
            A Sequence object created using the input dictionary; this will fail if the required properties are missing.
        """
        return cls._from_model(Sequence.from_dict(obj))


Sequence._model_class = SequenceModel
