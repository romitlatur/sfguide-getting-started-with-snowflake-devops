"""
Snowflake Secret API.

The Snowflake Secret API is a REST API that you can use to access, update, and perform certain actions on Secret resource in a Snowflake database.  # noqa: E501

The version of the OpenAPI document: 0.0.1
Contact: support@snowflake.com
Generated by: https://openapi-generator.tech

Do not edit this file manually.
"""

from __future__ import annotations

import json
import pprint
import re  # noqa: F401

from datetime import datetime
from typing import Any, Optional

from pydantic import StrictInt, StrictStr

from snowflake.core.secret._generated.models.secret import Secret


class JwtKeyPairSecret(Secret):
    """A model object representing the JwtKeyPairSecret resource.

    Constructs an object of type JwtKeyPairSecret with the provided properties.

    Parameters
    __________

    name : str
        Name of the secret

    comment : str, optional
        user comment associated to an object in the dictionary

    created_on : datetime, optional
        Date and time when the secret was created.

    database_name : str, optional
        Database in which the secret is stored

    schema_name : str, optional
        Schema in which the secret is stored

    owner : str, optional
        Role that owns the secret

    owner_role_type : str, optional
        The type of role that owns the secret

    algorithm : str, optional
        Key secret's algorithm name
    key_length : int, optional
        Size of key used in secret.
    """

    algorithm: Optional[StrictStr] = None

    key_length: Optional[StrictInt] = None

    __properties = ["type", "comment", "name", "created_on", "database_name", "schema_name", "owner", "owner_role_type"]

    class Config:
        populate_by_name = True
        validate_assignment = True

    def to_str(self) -> str:
        """Returns the string representation of the model using alias."""
        return pprint.pformat(self.dict(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias."""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> JwtKeyPairSecret:
        """Create an instance of JwtKeyPairSecret from a JSON string."""
        return cls.from_dict(json.loads(json_str))

    def to_dict(
        self,
        hide_readonly_properties: bool = False,
    ) -> dict[str, Any]:
        """Returns the dictionary representation of the model using alias."""
        exclude_properties = set()

        if hide_readonly_properties:
            exclude_properties.update({})

        _dict = dict(self._iter(to_dict=True, by_alias=True, exclude=exclude_properties, exclude_none=True))

        _dict["type"] = Secret.get_child_model_discriminator_value("JwtKeyPairSecret")

        return _dict

    def to_dict_without_readonly_properties(self) -> dict[str, Any]:
        """Return the dictionary representation of the model without readonly properties."""
        return self.to_dict(hide_readonly_properties=True)

    @classmethod
    def from_dict(cls, obj: dict) -> JwtKeyPairSecret:
        """Create an instance of JwtKeyPairSecret from a dict."""
        if obj is None:
            return None

        if type(obj) is not dict:
            return JwtKeyPairSecret.parse_obj(obj)

        _obj = JwtKeyPairSecret.parse_obj(
            {
                "comment": obj.get("comment"),
                "name": obj.get("name"),
                "created_on": obj.get("created_on"),
                "database_name": obj.get("database_name"),
                "schema_name": obj.get("schema_name"),
                "owner": obj.get("owner"),
                "owner_role_type": obj.get("owner_role_type"),
                "algorithm": obj.get("algorithm"),
                "key_length": obj.get("key_length"),
            }
        )

        return _obj


class JwtKeyPairSecretModel(Secret):
    def __init__(
        self,
        name: str,  # optional properties
        comment: Optional[str] = None,
        created_on: Optional[datetime] = None,
        database_name: Optional[str] = None,
        schema_name: Optional[str] = None,
        owner: Optional[str] = None,
        owner_role_type: Optional[str] = None,
        algorithm: Optional[str] = None,
        key_length: Optional[int] = None,
    ):
        """A model object representing the JwtKeyPairSecret resource.

        Constructs an object of type JwtKeyPairSecret with the provided properties.

        Parameters
        __________

        name : str
            Name of the secret

        comment : str, optional
            user comment associated to an object in the dictionary

        created_on : datetime, optional
            Date and time when the secret was created.

        database_name : str, optional
            Database in which the secret is stored

        schema_name : str, optional
            Schema in which the secret is stored

        owner : str, optional
            Role that owns the secret

        owner_role_type : str, optional
            The type of role that owns the secret

        algorithm : str, optional
            Key secret's algorithm name
        key_length : int, optional
            Size of key used in secret.
        """
        super().__init__(
            comment=comment,
            name=name,
            created_on=created_on,
            database_name=database_name,
            schema_name=schema_name,
            owner=owner,
            owner_role_type=owner_role_type,
        )
        self.algorithm = algorithm
        self.key_length = key_length

    __properties = ["type", "comment", "name", "created_on", "database_name", "schema_name", "owner", "owner_role_type"]

    def __repr__(self) -> str:
        return repr(self._to_model())

    def _to_model(self):
        return JwtKeyPairSecret(
            comment=self.comment,
            name=self.name,
            created_on=self.created_on,
            database_name=self.database_name,
            schema_name=self.schema_name,
            owner=self.owner,
            owner_role_type=self.owner_role_type,
            algorithm=self.algorithm,
            key_length=self.key_length,
        )

    @classmethod
    def _from_model(cls, model) -> JwtKeyPairSecretModel:
        return JwtKeyPairSecretModel(
            comment=model.comment,
            name=model.name,
            created_on=model.created_on,
            database_name=model.database_name,
            schema_name=model.schema_name,
            owner=model.owner,
            owner_role_type=model.owner_role_type,
            algorithm=model.algorithm,
            key_length=model.key_length,
        )

    def to_dict(self):
        """Create a dictionary of the properties from a JwtKeyPairSecret.

        This method constructs a dictionary with the key-value entries corresponding to the properties of the JwtKeyPairSecret object.

        Returns
        _______
        dict
            A dictionary object created using the input model.
        """
        return self._to_model().to_dict()

    @classmethod
    def from_dict(cls, obj: dict) -> JwtKeyPairSecretModel:
        """Create an instance of JwtKeyPairSecret from a dict.

        This method constructs a JwtKeyPairSecret object from a dictionary with the key-value pairs of its properties.

        Parameters
        ----------
        obj : dict
            A dictionary whose keys and values correspond to the properties of the resource object.

        Returns
        _______
        JwtKeyPairSecret
            A JwtKeyPairSecret object created using the input dictionary; this will fail if the required properties are missing.
        """
        return cls._from_model(JwtKeyPairSecret.from_dict(obj))


JwtKeyPairSecret._model_class = JwtKeyPairSecretModel
